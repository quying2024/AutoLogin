# 日志系统配置说明

## 📋 概述

项目已集成完整的日志系统，支持将所有调试信息记录到日志文件，同时可以选择是否在控制台显示输出。

## 🗂️ 文件结构

```
Autologin/
├── logs/                          # 日志目录（自动创建）
│   ├── autologin_2025-10-18.log  # 按日期命名的日志文件
│   ├── autologin_2025-10-17.log
│   └── ...
├── logger_config.py               # 日志配置模块
├── log_viewer.py                  # 日志查看工具
└── main.py                        # 主程序（已集成日志）
```

## ⚙️ 控制台输出开关

在 `main.py` 的顶部有一个全局配置：

```python
# 控制台输出开关（发布版本设置为False）
CONSOLE_OUTPUT = False  # True: 显示控制台输出, False: 仅记录到日志文件
```

### 开发模式（显示控制台输出）
```python
CONSOLE_OUTPUT = True
```
- ✅ 控制台显示所有调试信息
- ✅ 同时记录到日志文件
- 💡 适合开发和调试

### 发布模式（关闭控制台输出）
```python
CONSOLE_OUTPUT = False  # 默认设置
```
- ✅ 控制台无输出，界面清爽
- ✅ 所有信息记录到日志文件
- 💡 适合最终用户使用

## 📝 日志功能特性

### 自动功能
- ✅ 按日期自动生成日志文件
- ✅ 自动清理7天前的旧日志
- ✅ UTF-8编码，支持中文
- ✅ 包含时间戳和日志级别

### 日志级别
- `DEBUG` - 详细调试信息
- `INFO` - 一般信息
- `WARNING` - 警告信息
- `ERROR` - 错误信息
- `CRITICAL` - 严重错误

### 日志格式
```
2025-10-18 14:30:25 - INFO - 调试: 启动主程序
2025-10-18 14:30:26 - INFO - 调试: 窗口定位到右下角 (1521, 747)
2025-10-18 14:30:30 - INFO - 验证码识别成功: 566g
2025-10-18 14:30:35 - ERROR - 登录失败: 验证码错误
```

## 🔍 查看日志

### 方法1：使用日志查看工具
```powershell
.\venv\Scripts\python.exe log_viewer.py
```

功能菜单：
1. 查看日志文件列表
2. 查看最新日志（最后50行）
3. 查看今天的完整日志
4. 搜索日志
5. 日志统计
0. 退出

### 方法2：直接查看文件
日志文件位于 `logs/` 目录：
```powershell
# 使用记事本打开今天的日志
notepad logs\autologin_2025-10-18.log

# 使用VSCode打开
code logs\autologin_2025-10-18.log
```

### 方法3：使用PowerShell命令
```powershell
# 查看最后20行
Get-Content logs\autologin_2025-10-18.log -Tail 20

# 实时监控日志（类似tail -f）
Get-Content logs\autologin_2025-10-18.log -Wait

# 搜索包含"错误"的行
Select-String -Path logs\*.log -Pattern "错误"
```

## 🚀 发布准备

### 发布前检查清单

1. **关闭控制台输出**
   ```python
   # main.py 第23行
   CONSOLE_OUTPUT = False  ✅
   ```

2. **测试日志功能**
   ```powershell
   .\venv\Scripts\python.exe main.py
   # 检查 logs/ 目录是否生成日志文件
   ```

3. **清理测试日志**（可选）
   ```powershell
   Remove-Item logs\*.log
   ```

4. **更新README**
   - 添加日志说明
   - 说明日志位置
   - 提供查看方法

### 发布版本特性

✅ **用户友好**
- 无控制台输出干扰
- 界面清爽专业
- 后台静默运行

✅ **便于支持**
- 完整的操作日志
- 错误可追溯
- 方便远程诊断

✅ **自动维护**
- 自动清理旧日志
- 防止磁盘占用过多

## 📊 日志查看示例

### 查看今天的登录记录
```powershell
python log_viewer.py
# 选择: 3 - 查看今天的完整日志
```

### 搜索错误信息
```powershell
python log_viewer.py
# 选择: 4 - 搜索日志
# 输入: error 或 错误
```

### 查看最新操作
```powershell
python log_viewer.py
# 选择: 2 - 查看最新日志（最后50行）
```

## 🔧 高级配置

### 修改日志保留天数
编辑 `logger_config.py`:
```python
# 在模块末尾
clean_old_logs(days=7)  # 改为需要的天数，如 30
```

### 修改日志格式
编辑 `logger_config.py`:
```python
formatter = logging.Formatter(
    '%(asctime)s - %(levelname)s - %(message)s',
    datefmt='%Y-%m-%d %H:%M:%S'
)
```

### 修改日志目录
创建LoggerConfig实例时指定：
```python
logger_config = LoggerConfig(log_dir="mylogs")
```

## ⚠️ 注意事项

1. **日志文件编码**
   - 使用UTF-8编码
   - 支持中文内容
   - 使用记事本打开时注意编码设置

2. **日志文件大小**
   - 按天分割，单个文件不会过大
   - 自动清理旧文件
   - 一般情况下不会占用太多空间

3. **隐私保护**
   - 日志包含用户身份证号码
   - 密码已脱敏（仅显示前3位）
   - 发布时请注意保护用户隐私

4. **性能影响**
   - 日志记录对性能影响极小
   - 文件I/O异步处理
   - 不会影响用户体验

## 📚 使用示例

### 开发阶段
```python
# main.py
CONSOLE_OUTPUT = True  # 开启控制台输出

# 运行程序
python main.py
# 可以看到所有调试信息
```

### 发布版本
```python
# main.py  
CONSOLE_OUTPUT = False  # 关闭控制台输出

# 运行程序
python main.py
# 控制台无输出，专业界面

# 查看日志
python log_viewer.py
# 选择相应功能查看日志
```

## 🎯 最佳实践

1. **发布前**
   - 设置 `CONSOLE_OUTPUT = False`
   - 测试日志功能正常
   - 清理测试日志文件

2. **用户支持**
   - 要求用户提供日志文件
   - 使用log_viewer.py快速定位问题
   - 搜索关键错误信息

3. **版本更新**
   - 保持日志格式一致性
   - 添加新功能时记录相应日志
   - 重要操作必须记录

---
*版本：1.0*
*更新时间：2025年10月18日*
