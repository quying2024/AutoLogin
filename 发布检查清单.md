# 发布前检查清单

## ✅ 必检项目

### 1. 代码配置检查

- [ ] **控制台输出已关闭**
  ```python
  # main.py 第23行
  CONSOLE_OUTPUT = False  # 必须为False
  ```

- [ ] **日志系统正常**
  ```powershell
  python test_logging.py
  # 确保所有项都显示 ✅
  ```

- [ ] **语法检查通过**
  ```powershell
  python -m py_compile main.py
  python -m py_compile logger_config.py
  python -m py_compile captcha_handler.py
  ```

### 2. 功能测试

- [ ] **基本功能测试**
  - 启动程序正常
  - 窗口定位正确（右下角）
  - 浏览器最大化启动
  - 验证码识别正常
  - 自动登录成功
  - 浏览器独立运行
  - 程序退出无错误

- [ ] **日志功能测试**
  - logs目录自动创建
  - 日志文件正常生成
  - 日志内容完整
  - 旧日志自动清理
  - log_viewer.py正常工作

### 3. 文件清理

- [ ] **删除测试文件**
  ```powershell
  # 可选：删除测试脚本
  Remove-Item test_*.py
  Remove-Item replace_prints.py
  Remove-Item 窗口定位修改完成.py
  Remove-Item EPIPE错误*.py
  Remove-Item 功能恢复验证.py
  Remove-Item 浏览器独立运行*.py
  ```

- [ ] **清理测试日志**
  ```powershell
  # 可选：清理测试期间的日志
  Remove-Item logs\*.log
  ```

- [ ] **清理缓存文件**
  ```powershell
  Remove-Item __pycache__ -Recurse
  Remove-Item *.pyc -Recurse
  ```

### 4. 配置文件检查

- [ ] **config.json存在**
  - 包含默认配置
  - 超级鹰配置正确

- [ ] **README.md更新**
  - 添加日志系统说明
  - 更新使用说明
  - 添加故障排除指南

## 📦 发布文件清单

### 核心文件（必需）
```
✅ main.py                    # 主程序
✅ config.py                  # 配置管理
✅ captcha_handler.py         # 验证码处理
✅ settings_dialog.py         # 设置对话框
✅ logger_config.py           # 日志配置
✅ config.json                # 配置文件
✅ requirements.txt           # 依赖列表
```

### 工具文件（可选）
```
✅ log_viewer.py              # 日志查看工具
✅ 启动正常模式.bat           # 正常模式启动
✅ 启动调试模式.bat           # 调试模式启动
✅ 启动开发模式.bat           # 开发模式启动
✅ 查看日志.bat               # 日志查看工具启动
```

### 文档文件（推荐）
```
✅ README.md                  # 项目说明
✅ 使用说明.md                # 详细使用说明
✅ 日志系统说明.md            # 日志系统说明
✅ requirements.txt           # 依赖说明
```

### 自动生成文件（运行时）
```
📁 logs/                     # 日志目录（自动创建）
📁 venv/                     # 虚拟环境（用户自建）
📁 __pycache__/              # 缓存（可删除）
📄 app_config.txt            # 应用配置（自动生成）
📄 captcha.png               # 临时验证码（自动删除）
```

## 🧪 发布测试流程

### 第一步：清洁构建
```powershell
# 1. 清理所有缓存和临时文件
Remove-Item __pycache__ -Recurse -Force
Remove-Item logs\*.log
Remove-Item captcha.png -ErrorAction SilentlyContinue

# 2. 检查CONSOLE_OUTPUT设置
Select-String -Path main.py -Pattern "CONSOLE_OUTPUT"
# 确认显示: CONSOLE_OUTPUT = False
```

### 第二步：功能测试
```powershell
# 1. 启动程序
.\启动正常模式.bat

# 2. 测试登录功能
# - 输入测试账号姓名
# - 等待自动登录完成
# - 验证浏览器独立运行
# - 关闭主程序窗口
# - 确认浏览器继续运行

# 3. 检查日志
.\查看日志.bat
# - 选择查看最新日志
# - 确认有完整操作记录
```

### 第三步：异常测试
```powershell
# 测试各种异常情况
# - 错误的账号名
# - 网络断开
# - 验证码识别失败
# - Excel文件格式错误

# 检查日志记录
# - 确认错误被正确记录
# - 日志包含足够的调试信息
```

## 📝 发布说明模板

```
职称申报登录助手 v1.0

【新功能】
✨ 完整的日志系统
✨ 浏览器最大化启动
✨ 主窗口右下角定位
✨ 浏览器独立运行
✨ 自动验证码识别
✨ 自动登录功能

【使用说明】
1. 双击"启动正常模式.bat"启动程序
2. 输入姓名，点击登录
3. 程序自动完成登录流程
4. 关闭主程序窗口，浏览器继续运行

【日志查看】
- 双击"查看日志.bat"打开日志查看工具
- 日志位置：logs/目录
- 自动保留最近7天的日志

【故障排除】
如遇问题，请：
1. 查看logs目录中的日志文件
2. 搜索"ERROR"或"错误"关键词
3. 联系技术支持并提供日志文件

【系统要求】
- Windows 10/11
- Python 3.7+
- 网络连接
```

## ✅ 最终检查

在发布前，完成以下最终检查：

- [ ] 所有代码已提交到版本控制
- [ ] README.md完整且准确
- [ ] 所有测试通过
- [ ] 日志功能正常
- [ ] 控制台输出已关闭
- [ ] 启动脚本可用
- [ ] 依赖包版本记录
- [ ] 发布说明已准备

## 🎯 发布命令

```powershell
# 创建发布包
$version = "v1.0"
$releaseDir = "release-$version"

# 创建发布目录
New-Item -ItemType Directory -Path $releaseDir

# 复制必需文件
Copy-Item main.py $releaseDir
Copy-Item config.py $releaseDir
Copy-Item captcha_handler.py $releaseDir
Copy-Item settings_dialog.py $releaseDir
Copy-Item logger_config.py $releaseDir
Copy-Item log_viewer.py $releaseDir
Copy-Item config.json $releaseDir
Copy-Item requirements.txt $releaseDir
Copy-Item *.bat $releaseDir
Copy-Item *.md $releaseDir

# 压缩发布包
Compress-Archive -Path $releaseDir -DestinationPath "职称申报登录助手-$version.zip"

Write-Host "✅ 发布包创建完成: 职称申报登录助手-$version.zip"
```

---
*检查清单版本：1.0*
*更新时间：2025年10月18日*
