# 职称申报登录助手 - 打包和发布指南

## 📦 打包说明

本项目使用 PyInstaller 将 Python 程序打包成 Windows exe 可执行文件。

### 环境要求

- Windows 操作系统
- Python 3.12
- 虚拟环境 (venv)
- 所有依赖包已安装

### 打包步骤

#### 方法一：使用自动打包脚本（推荐）

1. 双击运行 `打包程序.bat`
2. 等待打包完成（约2-5分钟）
3. 打包成功后会自动打开输出目录

#### 方法二：手动打包

```powershell
# 1. 激活虚拟环境
.\venv\Scripts\activate

# 2. 安装 PyInstaller（如果未安装）
pip install pyinstaller

# 3. 清理旧文件
Remove-Item -Recurse -Force build, dist -ErrorAction SilentlyContinue

# 4. 执行打包
pyinstaller --clean autologin.spec

# 5. 查看结果
explorer dist\职称申报登录助手\
```

### 打包配置说明

`autologin.spec` 配置文件包含以下关键设置：

- **console=False**: 无控制台窗口（纯GUI模式）
- **icon**: 可自定义程序图标（需要 .ico 文件）
- **hiddenimports**: 包含所有必要的模块
- **datas**: 包含配置文件和资源

### 输出文件结构

```
dist\职称申报登录助手\
├── 职称申报登录助手.exe          # 主程序
├── _internal\                    # 依赖库和资源
│   ├── playwright\              # Playwright 相关文件
│   ├── wx\                      # wxPython 库
│   ├── pandas\                  # Pandas 库
│   ├── config.py                # 配置模块
│   ├── captcha_handler.py       # 验证码处理模块
│   ├── logger_config.py         # 日志配置模块
│   └── ...                      # 其他依赖文件
└── README.md                     # 说明文档
```

## 📋 发布前检查清单

### 代码检查

- [ ] `CONSOLE_OUTPUT = False` （关闭控制台输出）
- [ ] 所有功能测试通过
- [ ] 日志系统正常工作
- [ ] 验证码识别功能正常
- [ ] 自动登录功能正常
- [ ] 窗口位置和大小正确
- [ ] 浏览器启动正常

### 配置文件检查

- [ ] `config.py` 包含正确的默认配置
- [ ] `settings.json` 模板文件准备好
- [ ] Excel 数据文件模板准备好

### 文档检查

- [ ] README.md 更新到最新版本
- [ ] 使用说明完整
- [ ] 常见问题说明清楚
- [ ] 联系方式正确

### 打包测试

- [ ] 在本机测试 exe 能正常启动
- [ ] 测试完整的登录流程
- [ ] 测试日志文件生成
- [ ] 测试配置文件读写
- [ ] 测试 Excel 文件读取

### 清理工作

- [ ] 删除测试日志文件
- [ ] 删除测试配置文件
- [ ] 删除个人敏感信息

## 🚀 发布步骤

### 1. 准备发布包

打包完成后，需要准备以下文件：

```
职称申报登录助手_v1.0.0\
├── 职称申报登录助手.exe          # 主程序（从 dist 目录复制）
├── _internal\                    # 依赖文件（从 dist 目录复制）
├── README.md                     # 使用说明
├── settings.json.example         # 配置文件模板
├── 用户数据模板.xlsx             # Excel 模板文件
└── 使用说明.pdf                  # 详细使用手册（可选）
```

### 2. 创建配置文件模板

创建 `settings.json.example`：

```json
{
    "超级鹰用户名": "your_username",
    "超级鹰密码": "your_password",
    "软件ID": "your_software_id",
    "验证码类型": "1902",
    "浏览器类型": "chrome",
    "Excel文件路径": "用户数据.xlsx",
    "超时时间": 60,
    "自动识别验证码": true,
    "自动登录": true
}
```

### 3. 创建 Excel 模板

创建包含以下列的 Excel 文件：
- 姓名
- 身份证号
- 密码
- 其他需要的字段

### 4. 编写使用说明

在 README.md 中包含：

1. **系统要求**
   - Windows 10 或更高版本
   - 需要安装 Chrome/Edge/Firefox 浏览器
   
2. **首次使用**
   - 安装 Playwright 浏览器
   - 配置 settings.json
   - 准备 Excel 数据文件

3. **使用步骤**
   - 启动程序
   - 选择浏览器
   - 加载 Excel 数据
   - 开始自动登录

4. **常见问题**
   - 验证码识别失败怎么办？
   - 浏览器启动失败怎么办？
   - 登录失败怎么办？

5. **技术支持**
   - 联系方式
   - 问题反馈渠道

### 5. 打包发布文件

#### 方法一：ZIP 压缩包

```powershell
# 使用 PowerShell 创建 ZIP 包
Compress-Archive -Path "职称申报登录助手_v1.0.0\*" -DestinationPath "职称申报登录助手_v1.0.0.zip"
```

#### 方法二：安装程序（可选）

使用 Inno Setup 或 NSIS 创建专业的安装程序。

## 🔧 常见问题和解决方案

### 打包失败

**问题**: PyInstaller 打包时报错

**解决方案**:
1. 确保所有依赖包已安装
2. 清理 build 和 dist 目录后重新打包
3. 检查 .spec 文件配置是否正确
4. 查看详细错误信息并针对性修复

### exe 文件过大

**问题**: 打包后的 exe 文件很大（可能 200MB+）

**解决方案**:
1. 这是正常的，因为包含了所有依赖库
2. 可以使用 UPX 压缩（已在 .spec 中启用）
3. 排除不需要的模块（如 tkinter、matplotlib）

### 运行时缺少模块

**问题**: exe 运行时提示缺少某个模块

**解决方案**:
1. 在 .spec 文件的 hiddenimports 中添加该模块
2. 重新打包

### Playwright 浏览器问题

**问题**: exe 运行时提示找不到浏览器

**解决方案**:
1. 在目标机器上运行: `playwright install chromium`
2. 或者使用系统已安装的 Chrome/Edge 浏览器

### 日志文件问题

**问题**: 程序运行时无法创建日志文件

**解决方案**:
1. 确保程序有写入权限
2. 检查 logs 目录是否能正常创建
3. 可以在 exe 同级目录手动创建 logs 目录

## 📊 打包优化建议

### 减小文件大小

1. **排除不需要的模块**
   ```python
   excludes=[
       'tkinter',
       'matplotlib',
       'numpy',  # 如果不需要
   ]
   ```

2. **启用 UPX 压缩**
   ```python
   upx=True,
   upx_exclude=[],
   ```

3. **单文件模式（可选）**
   - 修改 .spec 文件使用 `onefile` 模式
   - 但启动速度会变慢

### 提升启动速度

1. **使用文件夹模式**（当前配置）
   - 启动更快
   - 文件较多

2. **减少隐藏导入**
   - 只导入真正需要的模块

3. **优化代码**
   - 延迟导入不常用的模块

## 🎯 发布渠道

### 内部发布

1. 企业内网共享
2. 企业软件仓库
3. 邮件发送

### 公开发布

1. GitHub Releases
2. 企业官网下载
3. 软件分发平台

## 📄 版本管理

建议使用语义化版本号：

- **v1.0.0**: 初始发布版本
- **v1.1.0**: 新增功能
- **v1.0.1**: Bug 修复
- **v2.0.0**: 重大更新

每次发布时：
1. 更新版本号
2. 记录更新日志
3. 标记 Git 版本
4. 归档旧版本

## 🔐 安全注意事项

1. **不要在程序中硬编码**
   - 不要包含真实的账号密码
   - 不要包含 API 密钥

2. **配置文件保护**
   - settings.json 应由用户自己创建
   - 提供模板文件供参考

3. **日志文件**
   - 注意日志中不要记录敏感信息
   - 提醒用户保护日志文件

4. **数据文件**
   - Excel 文件包含敏感数据
   - 提醒用户妥善保管

## 📞 技术支持

如果在打包或发布过程中遇到问题，可以：

1. 查看打包日志文件
2. 检查 PyInstaller 官方文档
3. 搜索类似问题的解决方案
4. 联系技术支持

---

**最后更新**: 2025年10月18日
